[project]
name = "breona"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "deepagents>=0.6.3",
    "dotenv>=0.9.9",
    "langchain-openai>=1.2.2",
    "searxng-wrapper>=0.1.1",
]
